BEGIN TRY

BEGIN TRAN;

-- CreateTable
CREATE TABLE [dbo].[praktijk] (
    [id] NVARCHAR(1000) NOT NULL,
    CONSTRAINT [praktijk_pkey] PRIMARY KEY CLUSTERED ([id])
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
