BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[survey_antwoorden] ALTER COLUMN [antwoorden] NVARCHAR(max) NOT NULL;

-- AlterTable
ALTER TABLE [dbo].[vragenlijst] ALTER COLUMN [json] NVARCHAR(max) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
